all:
  children:
    redis:
      hosts:
        redis-hel-0:
          ansible_host: #{{ ip_address }}
          ansible_user: root
          mode: 'redis'
          init_cluster: 'true'
        redis-hel-1:
          ansible_host: #{{ ip_address }}
          ansible_user: root
          mode: 'redis'
          init_cluster: 'false'
        redis-hel-2:
          ansible_host: #{{ ip_address }}
          ansible_user: root
          mode: 'redis'
          init_cluster: 'false'

    monitoring:
      hosts:
        monitoring-hel-0:
          ansible_host: #{{ ip_address }}
          ansible_user: root
          mode: 'monitoring'
          init_cluster: 'false'
        monitoring-hel-1:
          ansible_host: #{{ ip_address }}
          ansible_user: root
          mode: 'monitoring'
          init_cluster: 'false'

